<UserControl x:Class="BookCollector.Old.ImportStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:BookCollector.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" TrueState="Visible" FalseState="Hidden"/>
    </UserControl.Resources>
    
    <StackPanel Orientation="Horizontal">
        <Image Source="../Images/appbar.chevron.right.png"
               Height="24"
               VerticalAlignment="Center"
               Visibility="{Binding IsActive, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        <TextBlock x:Name="DisplayName" Margin="5"/>
    </StackPanel>
</UserControl>
