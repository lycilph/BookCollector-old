<UserControl x:Class="BookCollector.Screens.Main.MainToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookCollector.Screens.Main"
             mc:Ignorable="d" 
             d:DesignHeight="300" 
             d:DesignWidth="300"
             d:DataContext="{d:DesignInstance local:MainToolsViewModel}"
             MinWidth="200">
    <StackPanel>
        <Button Margin="10,5"
                Content="Change Collection"
                Command="{Binding ChangeCollectionCommand}"/>
        
        <Button Margin="10,5"
                Content="Import"
                Command="{Binding ImportCommand}"/>
    </StackPanel>
</UserControl>
