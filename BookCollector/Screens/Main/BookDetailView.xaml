<UserControl x:Class="BookCollector.Screens.Main.BookDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:main="clr-namespace:BookCollector.Screens.Main"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance main:BookViewModel}">
    <StackPanel>
        <TextBlock Text="{Binding Title}" FontSize="18" TextWrapping="Wrap"/>
        <TextBlock Text="{Binding Authors}"/>
        <TextBlock Text="{Binding ISBN10}"/>
        <TextBlock Text="{Binding ISBN13}"/>
    </StackPanel>
</UserControl>
