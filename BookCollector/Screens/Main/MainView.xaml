<UserControl x:Class="BookCollector.Screens.Main.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:gong="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
             mc:Ignorable="d" 
             d:DesignHeight="300"
             d:DesignWidth="300">
    <Grid Margin="{StaticResource ScreenMargin}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="3*" MinWidth="200"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="2*" MinWidth="200"/>
        </Grid.ColumnDefinitions>

        <!-- Shelfs and actions -->
        <StackPanel Grid.Column="0" Margin="{StaticResource ScreenColumnMargin}">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="ShelfTextblock" Text="Shelves" FontSize="{StaticResource SidebarHeaderFontSize}"/>
                <Button x:Name="AddShelf"
                        Style="{StaticResource MetroCircleButtonStyle}" 
                        Height="{Binding ElementName=ShelfTextblock, Path=ActualHeight}"
                        Margin="20,0,0,0">
                    <Image Source="../../Images/appbar.add.png"/>
                </Button>
            </StackPanel>
            <Separator Margin="{StaticResource SeparatorMargin}"/>
            <ListBox x:Name="Shelves"
                     FocusVisualStyle="{x:Null}"
                     FontSize="{StaticResource SidebarItemFontSize}"
                     cal:Message.Attach="[Shortcut F2] = [EditShelf(Shelves.SelectedItem)]"
                     gong:DragDrop.IsDropTarget="True"
                     gong:DragDrop.DropHandler="{Binding}">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ContextMenu>
                    <ContextMenu cal:Action.TargetWithoutContext="{Binding Path=PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                        <MenuItem cal:Message.Attach="RemoveShelf" Header="Delete"/>
                    </ContextMenu>
                </ListBox.ContextMenu>
            </ListBox>
            
            <TextBlock Text="Actions" FontSize="{StaticResource SidebarHeaderFontSize}" Margin="0,20,0,0"/>
            <Separator Margin="{StaticResource SeparatorMargin}"/>
            <Button x:Name="Search" 
                    Content="Search" 
                    Style="{StaticResource ChromelessButtonStyle}" 
                    FontSize="{StaticResource SidebarItemFontSize}" 
                    HorizontalAlignment="Left"/>
            <Button x:Name="Import" 
                    Content="Import" 
                    Style="{StaticResource ChromelessButtonStyle}" 
                    FontSize="{StaticResource SidebarItemFontSize}" 
                    HorizontalAlignment="Left"/>
            <Button x:Name="Export" 
                    Content="Export" 
                    Style="{StaticResource ChromelessButtonStyle}" 
                    FontSize="{StaticResource SidebarItemFontSize}" 
                    HorizontalAlignment="Left"/>
        </StackPanel>
        
        <!-- Books -->
        <ListBox Grid.Column="1" 
                 x:Name="SelectedShelf_Books" 
                 Style="{StaticResource VirtualisedMetroListBox}"
                 Margin="{StaticResource ScreenColumnMargin}"
                 gong:DragDrop.IsDragSource="True"/>
        
        <GridSplitter Grid.Column="2"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Stretch"
                      Width="{StaticResource GridSplitterSize}"/>
        
        <!-- Book details -->
        <controls:TransitioningContentControl Grid.Column="3" 
                                              x:Name="SelectedShelf_SelectedBook"
                                              cal:View.Context="Detail"
                                              Margin="{StaticResource ScreenColumnMargin}"/>
    </Grid>
</UserControl>
