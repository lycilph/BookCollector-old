<UserControl x:Class="BookCollector.Screens.MissingImages.MissingImagesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel Margin="20">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <Button x:Name="Back" Style="{DynamicResource MetroCircleButtonStyle}" VerticalAlignment="Bottom">
                <Image Source="../../Images/appbar.chevron.left.png" Width="40"/>
            </Button>
            <TextBlock Text="Missing Images" FontSize="{StaticResource HeaderFontSize}" Margin="10,0,0,0"/>
        </StackPanel>
        
        
    </DockPanel>
</UserControl>
