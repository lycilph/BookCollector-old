<UserControl x:Class="BookCollector.Screens.Selection.CollectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:panda="http://www.lycilph.com/Panda"
             xmlns:selection="clr-namespace:BookCollector.Screens.Selection"
             mc:Ignorable="d" 
             d:DesignHeight="300" 
             d:DesignWidth="300" 
             d:DataContext="{d:DesignInstance selection:CollectionViewModel}">
    <panda:EditableTextControl Text="{Binding Name}" IsEditing="{Binding IsEditing}"/>
</UserControl>
