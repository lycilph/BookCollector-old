<UserControl x:Class="BookCollector.Screens.Profiles.CollectionDescriptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:Framework.UI;assembly=Framework.UI"
             xmlns:profiles="clr-namespace:BookCollector.Screens.Profiles"
             mc:Ignorable="d" 
             d:DesignHeight="300" 
             d:DesignWidth="300" 
             d:DataContext="{d:DesignInstance profiles:CollectionDescriptionViewModel}">
    <StackPanel>
        <controls:EditableTextControl Text="{Binding DisplayName}"/>
        <TextBlock x:Name="Info"/>
        <TextBlock x:Name="Dates"/>
    </StackPanel>
</UserControl>
