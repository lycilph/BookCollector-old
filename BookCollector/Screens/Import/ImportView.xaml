<UserControl x:Class="BookCollector.Screens.Import.ImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <Button x:Name="Back" Style="{DynamicResource MetroCircleButtonStyle}" VerticalAlignment="Bottom">
                <Image Source="../../Images/appbar.chevron.left.png" Width="40"/>
            </Button>
            <TextBlock Text="Import" FontSize="{StaticResource HeaderFontSize}" Margin="20,0,0,0"/>
        </StackPanel>
        
        <ContentControl x:Name="ActiveItem" Margin="20"/>
    </DockPanel>
</UserControl>
