<UserControl x:Class="BookCollector.Screens.Search.SearchProviderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:local="clr-namespace:BookCollector.Screens.Search"
             xmlns:resources="clr-namespace:BookCollector.Resources"
             mc:Ignorable="d" 
             d:DesignHeight="300" 
             d:DesignWidth="300" 
             d:DataContext="{d:DesignInstance local:SearchProviderViewModel}">
    <UserControl.Resources>
        <resources:IsBusyOpacityConverter x:Key="IsBusyOpacityConverter" BusyOpacity="0.5"/>
    </UserControl.Resources>
    
    <Grid>
        <Image x:Name="Image" Opacity="{Binding IsBusy, Converter={StaticResource IsBusyOpacityConverter}}"/>
        <controls:ProgressRing IsActive="{Binding IsBusy}"
                               Width="{Binding ElementName=Image, Path=ActualWidth}"
                               Height="{Binding ElementName=Image, Path=ActualHeight}"/>
    </Grid>
</UserControl>
