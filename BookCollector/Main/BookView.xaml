<UserControl x:Class="BookCollector.Main.BookView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignWidth="300">
    <DockPanel LastChildFill="False">
        <Separator DockPanel.Dock="Bottom" Background="{StaticResource GrayBrush8}" Margin="0,0,10,0"/>
        <Image DockPanel.Dock="Left"
               Height="100"
               Width="65"
               Margin="0,5"
               Source="{Binding Image, TargetNullValue={x:Null}}"/>
        <TextBlock DockPanel.Dock="Top" x:Name="Title" Margin="0,5,0,0" Style="{StaticResource ContentTextBlockStyle}"/>
        <TextBlock DockPanel.Dock="Top" x:Name="Author" Style="{StaticResource ContentSmallTextBlockStyle}"/>
    </DockPanel>
</UserControl>
