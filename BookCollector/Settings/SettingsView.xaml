<UserControl x:Class="BookCollector.Settings.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:settings="clr-namespace:BookCollector.Settings"
             mc:Ignorable="d" 
             d:DesignHeight="300"
             d:DesignWidth="300"
             d:DataContext="{d:DesignInstance settings:SettingsViewModel}">
    <StackPanel x:Name="layout_root" Margin="20">
        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock Text="Keep Start Open" VerticalAlignment="Center"/>
            <controls:ToggleSwitchButton x:Name="KeepStartOpen" Margin="10,0,0,0"/>
        </StackPanel>
    </StackPanel>
</UserControl>
