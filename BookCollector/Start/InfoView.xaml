<UserControl x:Class="BookCollector.Start.InfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:start="clr-namespace:BookCollector.Start"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance start:InfoViewModel}">
    <DockPanel Width="200" LastChildFill="False" Background="Transparent">
        <StackPanel DockPanel.Dock="Right" VerticalAlignment="Bottom">
            <TextBlock Text="Updated" Style="{StaticResource ContentSmallTextBlockStyle}" HorizontalAlignment="Right"/>
            <TextBlock x:Name="Updated" Style="{StaticResource ContentSmallTextBlockStyle}" HorizontalAlignment="Right"/>
        </StackPanel>
        <TextBlock DockPanel.Dock="Top" x:Name="DisplayName" Style="{StaticResource SubHeaderTextBlockStyle}"/>
        <TextBlock x:Name="Text" Style="{StaticResource ContentTextBlockStyle}"/>
    </DockPanel>
</UserControl>
