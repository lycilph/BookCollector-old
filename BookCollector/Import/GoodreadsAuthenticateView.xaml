<UserControl x:Class="BookCollector.Import.GoodreadsAuthenticateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:utilities="clr-namespace:BookCollector.Utilities"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="VisibilityConverter"/>
    </UserControl.Resources>
    
    <DockPanel>
        <StackPanel DockPanel.Dock="Top">
            <TextBlock Text="Authenticating" HorizontalAlignment="Center"/>
            <TextBlock x:Name="Message" HorizontalAlignment="Center"/>
            <controls:MetroProgressBar IsIndeterminate="True"/>
        </StackPanel>
        <WebBrowser x:Name="Browser"
                    Visibility="{Binding ShowBrowser, Converter={StaticResource VisibilityConverter}}"
                    utilities:WebBrowserUtility.Source="{Binding Url}"
                    cal:Message.Attach="[Navigating] = [Navigating($eventArgs)]"/>
    </DockPanel>
</UserControl>
