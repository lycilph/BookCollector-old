<UserControl x:Class="BookCollector.Views.Screens.NavigationScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookCollector.Views.Screens"
             mc:Ignorable="d" 
             d:DesignHeight="300"
             d:DesignWidth="300"
             MinWidth="{StaticResource OverlayMinWidth}">
    <StackPanel Margin="32">
        <Button Content="Import" 
                HorizontalAlignment="Left"
                Command="{Binding ImportCommand}"
                Style="{StaticResource MaterialDesignFlatButton}"/>
        <Button Content="Goodreads" 
                Margin="0,32,0,0"
                HorizontalAlignment="Left"
                Command="{Binding GoodreadsCommand}"
                Style="{StaticResource MaterialDesignFlatButton}"/>
        <Button Content="Google Play"
                Margin="0,16,0,0"
                HorizontalAlignment="Left"
                Command="{Binding GooglePlayCommand}"
                Style="{StaticResource MaterialDesignFlatButton}"/>
    </StackPanel>
</UserControl>
