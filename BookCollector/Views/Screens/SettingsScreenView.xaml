<UserControl x:Class="BookCollector.Views.Screens.SettingsScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:BookCollector.ViewModels.Screens"
             mc:Ignorable="d" 
             d:DesignHeight="300"
             d:DesignWidth="300"
             d:DataContext="{d:DesignInstance vm:SettingsScreenViewModel}"
             MinWidth="{StaticResource OverlayMinWidth}">
    <StackPanel Margin="32">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Grid.ColumnSpan="2"
                       Text="Options"
                       Style="{StaticResource MaterialDesignSubheadingTextBlock}"/>

            <TextBlock Grid.Row="1"
                       Grid.Column="0"
                       Margin="0,8,0,0"
                       Text="Load on start" 
                       VerticalAlignment="Center"/>
            <ToggleButton Grid.Row="1"
                          Grid.Column="1"
                          Margin="0,8,0,0"
                          IsChecked="{Binding LoadCollectionOnStartup}"
                          Style="{StaticResource MaterialDesignSwitchAccentToggleButton}"/>

            <Separator Grid.Row="2" Grid.ColumnSpan="2" Margin="8,16" Style="{StaticResource MaterialDesignDarkSeparator}"/>

            <TextBlock Grid.Row="3"
                       Grid.ColumnSpan="2"
                       Text="Theme"
                       Style="{StaticResource MaterialDesignSubheadingTextBlock}"/>

            <TextBlock Grid.Row="4" 
                       Grid.Column="0" 
                       Margin="0,8,0,0"
                       Text="Primary" 
                       VerticalAlignment="Center"/>
            <ComboBox Grid.Row="4"
                      Grid.Column="2"
                      Margin="0,8,0,0"
                      IsEditable="False" 
                      md:HintAssist.Hint="Search"
                      md:HintAssist.HintOpacity=".26"
                      ItemsSource="{Binding PrimaryColors}"
                      SelectedItem="{Binding SelectedPrimaryColor}"
                      DisplayMemberPath="Name"/>

            <TextBlock Grid.Row="5" 
                       Grid.Column="0" 
                       Margin="0,8,0,0"
                       Text="Accent" 
                       VerticalAlignment="Center"/>
            <ComboBox Grid.Row="5"
                      Grid.Column="2"
                      Margin="0,8,0,0"
                      IsEditable="False" 
                      md:HintAssist.Hint="Search"
                      md:HintAssist.HintOpacity=".26"
                      ItemsSource="{Binding AccentColors}"
                      SelectedItem="{Binding SelectedAccentColor}"
                      DisplayMemberPath="Name"/>
        </Grid>
    </StackPanel>
</UserControl>
