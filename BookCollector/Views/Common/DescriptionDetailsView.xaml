<UserControl x:Class="BookCollector.Views.Common.DescriptionDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:BookCollector.ViewModels.Common"
             mc:Ignorable="d" 
             d:DesignHeight="300"
             d:DesignWidth="300"
             d:DataContext="{d:DesignInstance vm:DescriptionViewModel}"
             ToolTip="{Binding Filename}">
    <StackPanel>
        <TextBlock Text="{Binding Name}" Style="{StaticResource MaterialDesignHeadlineTextBlock}"/>
        <TextBlock Text="{Binding Text}" Margin="32,0,0,0"/>
        <TextBlock Text="{Binding Details}" Margin="32,0,0,0"/>
        <TextBlock Margin="32,0,0,0">
            <Run Text="Last Modified: "/>
            <Run Text="{Binding LastModifiedDate, Mode=OneWay}"/>
        </TextBlock>
    </StackPanel>
</UserControl>
