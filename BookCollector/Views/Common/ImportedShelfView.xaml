<UserControl x:Class="BookCollector.Views.Common.ImportedShelfView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookCollector.Views.Common"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Border Padding="8"
            BorderBrush="{StaticResource MaterialDesignDivider}" 
            BorderThickness="0,0,0,1">
        <TextBlock>
            <Hyperlink IsEnabled="{Binding Enabled}" 
                       Style="{StaticResource MaterialDesignBody1Hyperlink}">
                <TextBlock Text="{Binding Name}"/>
            </Hyperlink>
        </TextBlock>
    </Border>
</UserControl>
