<UserControl x:Class="BookSearch.Screens.Main.SearchProviderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:main="clr-namespace:BookSearch.Screens.Main"
             xmlns:resources="clr-namespace:BookSearch.Resources"
             mc:Ignorable="d" 
             d:DesignHeight="300" 
             d:DesignWidth="300" 
             d:DataContext="{d:DesignInstance main:SearchProviderViewModel}">
    <UserControl.Resources>
        <resources:IsBusyOpacityConverter x:Key="IsBusyOpacityConverter" BusyOpacity="0.5"/>
    </UserControl.Resources>
    
    <Grid Margin="5">
        <Image x:Name="Image"
               Opacity="{Binding IsBusy, Converter={StaticResource IsBusyOpacityConverter}}"/>
        <controls:ProgressRing IsActive="{Binding IsBusy}"
                               Width="{Binding ElementName=Image, Path=ActualWidth}"
                               Height="{Binding ElementName=Image, Path=ActualHeight}"/>
    </Grid>
</UserControl>
